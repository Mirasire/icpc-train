#include <stdio.h>
#include <stdlib.h>

void Quick_sort(int *arr,int p,int r)
{
    if(p < r)
    {
        int q = parition
    }
}

int main()
{
    int n,*arr;
    scanf("%d",&n);
    while(n--)
    {
        int m;
        scanf("%d",&m);
        arr = (int *)malloc(sizeof(int)*m);
        Quick_sort(arr,0,m-1);
        free(arr);
    }
    return 0;
}
