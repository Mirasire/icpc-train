#include <bits/stdc++.h>
using namespace std;

const int maxn = 5e4+100, MM = 1e9;
int arr[maxn],diff[maxn],n,m,l;

bool check(int val) {
	int sum,cnt;
	sum = cnt = 0;
	for(int i=0; i<n; i++) {
		sum += diff[i];
		if(sum >= val) cnt++,sum = 0;
		//TODO
	}
}

int main() {
	return 0;
}
