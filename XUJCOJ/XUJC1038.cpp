#include <iostream>

using namespace std;

int main()
{
    int T;
    cin >> T;
    while(T--)
    {
        long long int N,M;
        bool flag = 0;
        cin >> N >> M;
        //N M
        if(N >= M*2) flag = 1;
    }
    return 0;
}
